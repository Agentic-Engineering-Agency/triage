apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-langfuse-web
  labels:
    app: langfuse-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.langfuseWeb.port | default 3000 }}
      protocol: TCP
  selector:
    app: langfuse-web
