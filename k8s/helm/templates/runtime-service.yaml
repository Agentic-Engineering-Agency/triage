apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-runtime
  labels:
    app: runtime
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.runtime.port | default 4111 }}
      protocol: TCP
  selector:
    app: runtime
