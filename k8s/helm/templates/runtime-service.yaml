apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-runtime
  labels:
    app: runtime
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: runtime
